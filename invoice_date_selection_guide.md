# راهنمای گزینه تاریخ در فرم ورودی

## ویژگی جدید اضافه شده
در فرم ورودی محصولات، **گزینه انتخاب تاریخ فاکتور** اضافه شده است.

## تغییرات پیاده‌سازی شده

### 1. فیلد تاریخ در فرم ورودی
- فیلد جدید: `<input type="date" id="entry-invoice-date">`
- مکان: بعد از فیلد تعداد، قبل از دکمه افزودن
- مقدار پیش‌فرض: تاریخ امروز

### 2. توابع جدید و اصلاح شده

#### `setDefaultInvoiceDate()`
این تابع تاریخ پیش‌فرض امروز را در فیلد تاریخ قرار می‌دهد.

#### `clearEntryForm()`
این تابع به‌روزرسانی شده تا شامل فیلد تاریخ باشد:
- dropdown محصول
- نام محصول
- رنگ محصول
- تعداد
- **تاریخ فاکتور**

#### `addEntry()`
این تابع به‌روزرسانی شده:
- دریافت تاریخ از فیلد
- ذخیره تاریخ در آیتم‌های موقت
- استفاده از تاریخ انتخابی کاربر

#### `saveEntry()`
این تابع به‌روزرسانی شده:
- استفاده از تاریخ اولین آیتم (همه آیتم‌ها همان تاریخ را دارند)
- ذخیره تاریخ در فاکتور
- **آیتم‌ها فقط شامل نام، رنگ و تعداد هستند** (تاریخ در سطح فاکتور ذخیره می‌شود)

#### `renderTempEntryItems()`
این تابع به‌روزرسانی شده:
- نمایش ستون جدید "تاریخ فاکتور"
- فرمت تاریخ به صورت قابل خواندن

#### `init()`
این تابع به‌روزرسانی شده:
- فراخوانی `setDefaultInvoiceDate()` در زمان لود صفحه

### 3. به‌روزرسانی UI
- **Header جدول لیست موقت** شامل ستون "تاریخ فاکتور"
- **ساختار لیست موقت** شامل ستون تاریخ

## نحوه استفاده

### ثبت ورودی با تاریخ مشخص:
1. فیلدهای محصول را پر کنید
2. **تاریخ مورد نظر** را از تقویم انتخاب کنید
3. محصول را به لیست موقت اضافه کنید
4. تعداد محصولات اضافه کنید
5. روی "💾 ذخیره فاکتور ورودی" کلیک کنید

### تاریخ پیش‌فرض:
- فیلد تاریخ به صورت خودکار بر روی **تاریخ امروز** تنظیم می‌شود
- می‌توانید تاریخ را به دلخواه تغییر دهید

## مزایای این ویژگی
- ✅ **تاریخ دقیق فاکتور**: امکان ثبت فاکتور برای تاریخ‌های گذشته
- ✅ **رهگیری زمانی**: پیگیری دقیق موجودی بر اساس تاریخ
- ✅ **مدیریت آسان**: انتخاب تاریخ از تقویم (بدون نیاز به تایپ)
- ✅ **رابط کاربری بهتر**: فرم کامل و قابل فهم
- ✅ **سازگاری کامل**: با سیستم قبلی هماهنگ است

## نمایش در لیست موقت
لیست موقت ورودی شامل ستون‌های زیر است:
- نام محصول
- رنگ
- تعداد
- **تاریخ فاکتور** (جدید)
- عملیات

## نکات فنی
- تاریخ در فرمت `YYYY-MM-DD` ذخیره می‌شود
- همه آیتم‌های یک فاکتور همان تاریخ را دارند
- تاریخ فاکتور در سطح فاکتور ذخیره می‌شود، نه در سطح آیتم
- اگر تاریخ خالی باشد، تاریخ امروز استفاده می‌شود

## تست عملکرد
- سرور در پورت 8003 در حال اجرا است
- آدرس: http://localhost:8003/index.html
- کامیت: 5ab7cd5 "Add invoice date selection to entry form"

## تست عملی
1. به تب "📦 ورودی" بروید
2. مشاهده کنید که فیلد "تاریخ فاکتور" وجود دارد
3. تاریخی را انتخاب کنید
4. محصول اضافه کنید
5. مشاهده کنید که در لیست موقت تاریخ نمایش داده می‌شود
6. فاکتور را ذخیره کنید
7. مشاهده کنید که فاکتور با تاریخ صحیح ذخیره شده
