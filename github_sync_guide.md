# راهنمای همگام‌سازی با گیت هاب

## 🔗 نحوه استفاده از همگام‌سازی GitHub در سیستم مدیریت انبار زنانه

### مرحله 1: ایجاد Personal Access Token در گیت هاب

1. **ورود به GitHub**: به [github.com](https://github.com) بروید و وارد حساب خود شوید
2. **دسترسی به تنظیمات**: روی عکس پروفایل خود کلیک کنید و "Settings" را انتخاب کنید
3. **Developer settings**: در منوی سمت چپ، "Developer settings" را کلیک کنید
4. **Personal access tokens**: "Personal access tokens" → "Tokens (classic)" را انتخاب کنید
5. **Generate new token**: روی "Generate new token" کلیک کنید
6. **تنظیم token**:
   - Note: "EliShoes Data Sync"
   - Expiration: مدت دلخواه خود را انتخاب کنید
   - Scopes: فقط `gist` را انتخاب کنید
7. **Generate**: روی "Generate token" کلیک کنید
8. **کپی token**: Token را کپی کنید (فقط یکبار نمایش داده می‌شود)

### مرحله 2: ایجاد Gist

1. **ورود به Gist**: به [gist.github.com](https://gist.github.com) بروید
2. **ایجاد گیست جدید**:
   - Description: "EliShoes - Backup of inventory and sales data"
   - نام فایل: `elishoes_data.json` (خودکار اضافه می‌شود)
   - محتوا: خالی باشد (سیستم خودش پر می‌کند)
3. **Create secret gist**: روی "Create secret gist" کلیک کنید
4. **کپی Gist ID**: از URL گیست، ID را کپی کنید (مثلاً از `https://gist.github.com/username/abcd1234567890`، ID برابر `abcd1234567890` است)

### مرحله 3: تنظیم در اپلیکیشن

1. **بازکردن تب مدیریت داده**: در اپلیکیشن، روی تب "☁️ مدیریت داده" کلیک کنید
2. **کلیک روی GitHub Gist**: روی دکمه "📱 GitHub Gist" کلیک کنید
3. **ورود Token**: Personal Access Token را در کادر اول وارد کنید
4. **ورود Gist ID**: Gist ID را در کادر دوم وارد کنید
5. **تست اتصال**: سیستم خودکار اتصال را تست می‌کند

### مرحله 4: استفاده از همگام‌سازی

#### 📤 ارسال داده به گیت هاب
- روی دکمه "⬆️ ارسال به GitHub" کلیک کنید
- تمام اطلاعات انبار، فاکتورها و فروش‌ها ذخیره می‌شود

#### 📥 دریافت داده از گیت هاب
- روی دکمه "⬇️ دریافت از GitHub" کلیک کنید
- تمام اطلاعات از گیت هاب بارگیری می‌شود

### ✅ ویژگی‌های سیستم

**🔒 امنیت**:
- Personal Access Token محرمانه نگه داشته می‌شود
- فقط دسترسی به Gist دارد
- اطلاعات در سرورهای GitHub ذخیره می‌شود

**🔄 همگام‌سازی خودکار**:
- داده‌ها به فرمت JSON ذخیره می‌شوند
- تاریخ آخرین همگام‌سازی نمایش داده می‌شود
- خطاها با پیام‌های راهنما نمایش داده می‌شوند

**📊 داده‌های همگام‌سازی شده**:
- لیست کامل محصولات و انبار
- تمام فاکتورهای ورود و فروش
- تاریخچه معاملات
- تنظیمات سیستم

### 🚨 نکات مهم

1. **Token محرمانه است**: هرگز Token را با دیگران به اشتراک نگذارید
2. **Gist خصوصی**: از Gist خصوصی (Secret) استفاده کنید
3. **اتصال اینترنت**: برای همگام‌سازی نیاز به اتصال اینترنت دارید
4. **Backup محلی**: همیشه backup محلی نگه دارید
5. **تست منظم**: عملکرد همگام‌سازی را به طور منظم بررسی کنید

### 🔧 رفع مشکلات

**خطای Token**:
- Token را دوباره تولید کنید
- اطمینان حاصل کنید که scope `gist` فعال است

**خطای Gist ID**:
- URL گیست را بررسی کنید
- اطمینان حاصل کنید که گیست وجود دارد

**خطای اتصال**:
- اتصال اینترنت را بررسی کنید
- فایروال را بررسی کنید

---

**نویسنده**: MiniMax Agent  
**تاریخ**: ۱۴۰۴/۰۸/۱۵  
**نسخه**: ۱.۰